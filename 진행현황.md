# 비닐하우스 농장 질병 감지 앱 — 진행 현황

**마지막 업데이트**: 2026-05-28  
**현재 단계**: 1주차 Day 1~2 완료, Flutter SDK 설치 중

---

## ✅ 완료된 작업

### 1. DB 스키마 (`schema.sql`)

**파일 위치**: `greenhouse/schema.sql`

PostgreSQL 테이블 3개 + 편의 뷰 1개 생성.

| 테이블 | 설명 |
|--------|------|
| `fields` | 밭(구역) 정보. 기본 5개 데이터 자동 삽입 |
| `analysis_results` | AI 분석 결과 저장 |
| `notifications` | 알림 발송 기록 |
| `v_field_latest_status` | 각 밭의 최신 분석 결과 뷰 |

추가 구현 사항:
- `fields.updated_at` 자동 갱신 트리거
- 조회 성능용 인덱스 (field_id, timestamp, disease_detected)
- 미확인 알림 전용 partial 인덱스

---

### 2. API 명세 문서 (`API_SPEC.md`)

**파일 위치**: `greenhouse/API_SPEC.md`

Flutter 앱에서 호출할 엔드포인트 5개 정의.

| 메서드 | 경로 | 설명 |
|--------|------|------|
| POST | `/analyze` | 이미지 분석 요청 & 결과 저장 |
| GET | `/history` | 분석 히스토리 조회 |
| GET | `/status/:field_id` | 특정 밭 현재 상태 조회 |
| GET | `/fields` | 전체 밭 목록 조회 |
| PATCH | `/notifications/:id/read` | 알림 읽음 처리 |

---

### 3. Python 백엔드 (`server/`)

**파일 위치**: `greenhouse/server/`  
**기술 스택**: FastAPI + SQLAlchemy (async) + PostgreSQL + Gemini AI

#### 폴더 구조

```
server/
├── main.py                 # FastAPI 앱 진입점, CORS, 서버 시작 시 밭 자동 시드
├── requirements.txt        # 의존성 패키지
├── .env                    # 환경변수 (DB 접속정보, Gemini API 키)
├── .env.example            # 환경변수 예시
├── core/
│   └── database.py         # PostgreSQL 비동기 연결 (asyncpg)
├── models/
│   └── models.py           # SQLAlchemy ORM 모델 (Field, Device, Image, AnalysisResult, Notification)
├── schemas/
│   └── schemas.py          # Pydantic 응답 스키마 (DTO)
└── routers/
    ├── api.py              # 전체 API 라우터 (5개 엔드포인트)
    └── seed.py             # 밭 초기 데이터 자동 생성
```

#### 주요 구현 내용

- **CORS 설정**: Flutter 앱(Android 에뮬레이터 포함) 연결 허용
- **Gemini Vision AI 연동**: 이미지 분석 → 질병 유형 + 신뢰도 반환
  - `GEMINI_API_KEY` 없을 시 mock 결과로 자동 fallback
- **밭 자동 시드**: 서버 시작 시 DB에 밭이 없으면 5개 자동 생성
- **질병 감지 시 자동 처리**: 알림 생성 + 밭 상태(NORMAL/WARNING/DANGER) 자동 업데이트

#### 감지 가능한 질병 유형

| 값 | 설명 |
|----|------|
| `NORMAL` | 정상 |
| `BLIGHT` | 역병 |
| `RUST` | 녹병 |
| `MOSAIC` | 모자이크바이러스 |
| `POWDERY_MILDEW` | 흰가루병 |
| `LEAF_SPOT` | 점무늬병 |
| `UNKNOWN` | 판별 불가 |

#### 서버 실행 방법

```bash
cd greenhouse/server
pip install -r requirements.txt
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

#### 환경변수 설정 (`.env`)

```
DB_HOST=localhost
DB_PORT=5432
DB_NAME=greenhouse_db
DB_USER=postgres
DB_PASSWORD=비밀번호
IMAGE_UPLOAD_DIR=./uploaded_images
GEMINI_API_KEY=Gemini_API_키
```

> Gemini API 키 발급: https://aistudio.google.com/app/apikey (무료)

---

## 🔄 진행 중인 작업

### 4. Flutter 앱 (`smart_farm`) 환경 설정

**저장 위치**: `C:\mobileprogram\smart_farm`

| 단계 | 상태 | 내용 |
|------|------|------|
| Flutter 플러그인 설치 | ✅ 완료 | Android Studio Plugins에서 설치 |
| Flutter SDK 다운로드 | 🔄 진행 중 | flutter.dev에서 zip 다운로드 |
| Flutter SDK 압축 해제 | ⬜ 대기 | `C:\flutter`에 압축 풀기 |
| 환경변수 등록 | ⬜ 대기 | PATH에 `C:\flutter\bin` 추가 |
| Android Studio SDK Path 설정 | ⬜ 대기 | SDK Path에 `C:\flutter` 입력 |
| Flutter 프로젝트 생성 | ⬜ 대기 | 프로젝트명: `smart_farm` |

#### Flutter SDK 설치 명령어 (SDK 설치 후 실행)

```powershell
# PATH 환경변수 등록
[System.Environment]::SetEnvironmentVariable("Path", $env:Path + ";C:\flutter\bin", "User")

# 설치 확인
flutter --version
```

---

## ⬜ 남은 작업

### Flutter 앱 내부 구조 (SDK 설치 완료 후 진행 예정)

```
smart_farm/lib/
├── models/              # DTO — API 응답을 Dart 객체로 변환
│   ├── field.dart
│   ├── analysis_result.dart
│   └── notification.dart
├── services/            # Adapter — HTTP 호출 담당
│   ├── api_client.dart
│   ├── field_service.dart
│   └── analyze_service.dart
├── repositories/        # 비즈니스 로직
│   └── plant_repository.dart
└── screens/             # UI 화면
```

### Flutter 앱에서 백엔드 연결 시 Base URL

| 환경 | URL |
|------|-----|
| Android 에뮬레이터 | `http://10.0.2.2:8000` |
| 실제 기기 | `http://컴퓨터IP:8000` |
| iOS 시뮬레이터 | `http://localhost:8000` |

---

## 📁 전체 프로젝트 구조

```
greenhouse/                     # 메인 레포지토리
├── schema.sql                  # ✅ PostgreSQL DB 스키마
├── API_SPEC.md                 # ✅ REST API 명세
├── server/                     # ✅ Python FastAPI 백엔드
│   ├── main.py
│   ├── core/database.py
│   ├── models/models.py
│   ├── schemas/schemas.py
│   └── routers/
│       ├── api.py
│       └── seed.py
└── ai/                         # ML팀 AI 모델 코드
    ├── smartfarm_yolo.py
    └── plant_disease_preprocessing.py

C:\mobileprogram\smart_farm\    # 🔄 Flutter 앱 (설정 중)
└── lib/                        # ⬜ 구조 세팅 예정
```

---

## 📌 참고 링크

- Flutter SDK 설치: https://flutter.dev/docs/get-started/install/windows
- Gemini API 키 발급: https://aistudio.google.com/app/apikey
- FastAPI 문서 (서버 실행 후): http://localhost:8000/docs
