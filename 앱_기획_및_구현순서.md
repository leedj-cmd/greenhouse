# 앱 기획 및 구현 순서

**업데이트**: 2026-05-28  
**담당**: 총괄 (AI 모델 제외 전체)

---

## 📱 앱 기획 (확정)

### 구역 구조
- 구역명: `a1, b1, c1, d1, e1` ~ (그리드 형태로 확장 가능)
- 각 구역마다 하루 1회 사진 촬영 → 서버 전송 → DB 저장

### 촬영 규칙
- **촬영 시간**: 매일 14:00 자동 촬영
- **촬영 주체**: 앱 카메라 (스케줄러가 자동 실행)
- **저장 위치**: 촬영 이미지 → 서버 API → DB

### 화면 구성

#### 하단 네비게이션 바 (2개 탭)
| 탭 | 아이콘 | 설명 |
|----|--------|------|
| Farm | 🌿 | 구역별 현황 화면 |
| Image | 🖼️ | 전체 사진 갤러리 |

#### Farm 화면
- 구역(a1, b1, c1...) 그리드 표시
- **정상 구역**: 초록색
- **질병 감지 구역**: 빨간색 + ⚠️ 경고 아이콘
- **구역 클릭** → 해당 구역 최신 사진 + 분석 결과 팝업

#### Image 화면
- 서버 DB에 저장된 전체 사진 목록 (최신순)
- 구역별 필터 가능
- 사진 클릭 → 상세 보기 (구역명, 촬영일시, 분석 결과)

---

## 🔢 구현 순서 (AI 모델 제외)

---

### STEP 1. 백엔드 DB & API 수정
> **이유**: 구역명이 `a1, b1...` 형태로 바뀌었으므로 백엔드부터 맞춰야 함

- [ ] `fields` 테이블 구역명 `a1~e1` 형태로 시드 데이터 수정
- [ ] `POST /analyze` — 구역 ID + 이미지 수신, 분석 결과 저장
- [ ] `GET /fields` — 전체 구역 목록 + 각 구역 최신 분석 결과 포함
- [ ] `GET /images` — 전체 이미지 목록 (최신순, 구역 필터)
- [ ] `GET /fields/{id}/latest-image` — 특정 구역 최신 이미지 조회

---

### STEP 2. Android 프로젝트 초기 세팅
> **이유**: 코드 작성 전 의존성·구조·권한을 먼저 잡아야 나중에 안 꼬임

- [ ] `build.gradle` 의존성 추가
  - Retrofit2 + OkHttp (API 통신)
  - Gson (JSON 파싱)
  - Glide (이미지 로딩)
  - WorkManager (14:00 자동 촬영 스케줄)
- [ ] `AndroidManifest.xml` 권한 추가
  - CAMERA, INTERNET, BLUETOOTH, RECEIVE_BOOT_COMPLETED
- [ ] `lib/` 패키지 구조 생성
  ```
  com.example.smartfarm/
  ├── model/        ← DTO
  ├── network/      ← Retrofit 인터페이스
  ├── repository/   ← API 호출 로직
  ├── ui/farm/      ← Farm 화면
  ├── ui/image/     ← Image 화면
  └── worker/       ← 자동 촬영 WorkManager
  ```

---

### STEP 3. 데이터 레이어 구성 (Model + Network + Repository)
> **이유**: UI보다 데이터 흐름을 먼저 만들어야 화면에서 갖다 쓸 수 있음

- [ ] **DTO 클래스** (model/)
  - `Field.java` — id, name, status, latestDisease, latestImageUrl
  - `AnalysisResult.java` — id, fieldId, diseaseType, confidence, analyzedAt
  - `ImageItem.java` — id, fieldId, fieldName, filePath, capturedAt
  - `Notification.java` — id, fieldId, message, isRead

- [ ] **Retrofit 인터페이스** (network/)
  - `ApiService.java` — 엔드포인트 5개 선언
  - `ApiClient.java` — Base URL, OkHttp 설정

- [ ] **Repository** (repository/)
  - `PlantRepository.java` — API 호출 + 결과 반환

---

### STEP 4. Farm 화면 구현
> **이유**: 핵심 화면이므로 먼저 구현하고 검증

- [ ] `FarmFragment.java` + `fragment_farm.xml`
  - 구역 그리드 (GridLayout 또는 RecyclerView)
  - 각 구역 카드: 구역명, 상태 색상, 경고 아이콘
- [ ] 구역 클릭 시 팝업 (BottomSheetDialog)
  - 구역 최신 사진 표시 (Glide)
  - 분석 결과 (질병명, 신뢰도, 촬영 시각)
- [ ] API 연동 — `/fields` 호출해서 구역 상태 불러오기
- [ ] 상태별 색상 처리
  - `NORMAL` → 초록
  - `WARNING` → 노랑 + ⚠️
  - `DANGER` → 빨강 + ⚠️

---

### STEP 5. Image 화면 구현

- [ ] `ImageFragment.java` + `fragment_image.xml`
  - RecyclerView 그리드 (2열)
  - 각 아이템: 썸네일, 구역명, 날짜
- [ ] 구역 필터 칩 (ChipGroup)
- [ ] 사진 클릭 → 상세 다이얼로그
  - 원본 사진, 구역명, 촬영시각, 질병 분석 결과
- [ ] API 연동 — `/images` 호출해서 목록 불러오기
- [ ] 무한 스크롤 (페이지네이션)

---

### STEP 6. 하단 네비게이션 바 연결

- [ ] `MainActivity.java` + `activity_main.xml`
  - `BottomNavigationView` 설정
  - Farm / Image Fragment 전환
- [ ] 아이콘 및 라벨 리소스 추가

---

### STEP 7. 14:00 자동 촬영 스케줄러

- [ ] `CameraWorker.java` (WorkManager)
  - 매일 14:00에 실행
  - 카메라 촬영 → 이미지 파일 생성
  - 서버 `/analyze`로 업로드
- [ ] 앱 시작 시 WorkManager 스케줄 등록
- [ ] 기기 재부팅 후에도 스케줄 유지 (BootReceiver)

---

### STEP 8. 카메라 모듈 구현

- [ ] CameraX 라이브러리 설정
- [ ] 촬영 후 이미지 파일 저장
- [ ] Multipart 형태로 서버에 업로드 (Retrofit)
- [ ] 촬영 구역 선택 로직 (순서대로 또는 사용자 선택)

---

### STEP 9. 알림 시스템

- [ ] 서버 `/notifications` 폴링 (30분마다)
  - 또는 FCM 푸시 알림 (선택)
- [ ] 질병 감지 시 상단 알림 표시
- [ ] 알림 탭 시 Farm 화면 해당 구역으로 이동
- [ ] 읽음 처리 (`PATCH /notifications/{id}/read`)

---

### STEP 10. ML팀 TFLite 모델 연동
> **이유**: 모델 완성 후 서버의 `_mock_analyze()` 함수를 교체

- [ ] ML팀에서 `.tflite` 모델 파일 수령
- [ ] 서버 `routers/api.py`의 `_mock_analyze()` 함수를 TFLite 추론으로 교체
- [ ] 모델 정확도 검증 (목표 > 85%)

---

### STEP 11. 테스트 & 마무리

- [ ] 전체 플로우 E2E 테스트 (촬영 → 분석 → 표시 → 알림)
- [ ] 실제 기기 테스트 (Android 8.0 이상)
- [ ] 성능 테스트 (배터리, 메모리)
- [ ] APK 빌드 및 배포

---

## 📊 우선순위 요약

| 순서 | 단계 | 난이도 | 예상 소요 |
|------|------|--------|-----------|
| 1 | 백엔드 API 수정 | 하 | 0.5일 |
| 2 | Android 프로젝트 세팅 | 하 | 0.5일 |
| 3 | 데이터 레이어 | 중 | 1일 |
| 4 | Farm 화면 | 중 | 1.5일 |
| 5 | Image 화면 | 하 | 1일 |
| 6 | 하단 네비게이션 | 하 | 0.5일 |
| 7 | 자동 촬영 스케줄러 | 상 | 1.5일 |
| 8 | 카메라 모듈 | 상 | 1일 |
| 9 | 알림 시스템 | 중 | 1일 |
| 10 | TFLite 연동 | 중 | 1일 |
| 11 | 테스트 & 배포 | - | 1일 |
